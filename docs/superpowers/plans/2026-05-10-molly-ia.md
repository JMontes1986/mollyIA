# Molly IA — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build Molly IA — a RAG-powered school chatbot for Colegio Gemelli parents, with admin panel, using Groq + ChromaDB + Next.js 14.

**Architecture:** FastAPI backend on Railway handles RAG (Groq + ChromaDB + sentence-transformers) and document ingestion from PDFs/web/social. Next.js 14 frontend on Vercel handles chat UI, admin panel, and auth via Clerk. Cloudflare R2 stores original PDF files.

**Tech Stack:** Python 3.11 · FastAPI · SQLAlchemy · Alembic · ChromaDB · sentence-transformers · PyMuPDF · Groq SDK · BeautifulSoup · Next.js 14 · TailwindCSS · Clerk · Cloudflare R2 SDK · PostgreSQL

---

## File Map

### Backend (`backend/`)
```
backend/
├── main.py                    # FastAPI app + router registration + CORS
├── config.py                  # Pydantic Settings — all env vars
├── database.py                # SQLAlchemy engine + SessionLocal + get_db
├── models.py                  # ORM: User, Conversation, Message, Document, ScrapeJob
├── schemas.py                 # Pydantic request/response schemas
├── auth.py                    # Clerk JWT verification dependency
├── rag/
│   ├── embedder.py            # sentence-transformers wrapper (singleton)
│   ├── vectorstore.py         # ChromaDB client wrapper
│   ├── retriever.py           # similarity search → top-5 chunks
│   └── generator.py           # Groq chat completion + Molly system prompt
├── ingest/
│   ├── chunker.py             # LangChain text splitter (500 tokens / 50 overlap)
│   ├── pdf_parser.py          # PyMuPDF → plain text
│   ├── docx_parser.py         # python-docx → plain text
│   └── storage.py             # Cloudflare R2 upload/download/delete
├── scrapers/
│   ├── web_scraper.py         # colgemelli.edu.co → text chunks
│   ├── facebook_scraper.py    # public FB page posts → text
│   ├── instagram_scraper.py   # public IG captions → text
│   └── youtube_scraper.py     # YouTube Data API v3 → titles + descriptions
├── routers/
│   ├── chat.py                # POST /chat
│   ├── documents.py           # GET/POST/DELETE /documents
│   ├── scrape.py              # POST /scrape, GET /scrape/jobs
│   └── stats.py               # GET /stats
├── migrations/                # Alembic auto-generated
├── tests/
│   ├── conftest.py            # pytest fixtures: test DB, test client
│   ├── test_rag.py            # embedder, retriever, generator unit tests
│   ├── test_ingest.py         # chunker, parsers unit tests
│   ├── test_scrapers.py       # scraper unit tests (mocked HTTP)
│   └── test_routers.py        # integration tests for all endpoints
├── requirements.txt
├── Dockerfile
└── railway.toml
```

### Frontend (`frontend/`)
```
frontend/
├── app/
│   ├── layout.tsx             # Root layout: fonts, ClerkProvider, globals
│   ├── globals.css            # Tailwind + CSS custom properties (paleta Gemelli)
│   ├── page.tsx               # Landing pública (/)
│   ├── (auth)/
│   │   └── sign-in/[[...sign-in]]/page.tsx   # Clerk hosted sign-in
│   ├── chat/
│   │   ├── layout.tsx         # Chat layout: sidebar + main area
│   │   ├── page.tsx           # Redirect a /chat/new o última conversación
│   │   └── [id]/
│   │       └── page.tsx       # Vista de conversación específica
│   ├── perfil/
│   │   └── page.tsx           # Perfil del usuario
│   └── admin/
│       ├── layout.tsx         # Admin layout + guard de rol
│       ├── page.tsx           # Dashboard KPIs
│       ├── documentos/page.tsx
│       ├── fuentes/page.tsx
│       ├── conversaciones/page.tsx
│       ├── frecuentes/page.tsx
│       └── usuarios/page.tsx
├── components/
│   ├── ui/
│   │   ├── MollyAvatar.tsx    # Pug logo en círculo navy con borde oro
│   │   ├── Button.tsx         # Variantes: primary, ghost, danger
│   │   └── StatusBadge.tsx    # indexed / processing / error
│   ├── chat/
│   │   ├── ChatSidebar.tsx    # Lista de conversaciones + nuevo chat
│   │   ├── ChatMessages.tsx   # Scroll container de mensajes
│   │   ├── MessageBubble.tsx  # Burbuja molly/user + source pills
│   │   ├── ChatInput.tsx      # Textarea + send button
│   │   └── SourcePills.tsx    # Chips de fuentes citadas
│   ├── landing/
│   │   ├── Hero.tsx           # Sección hero con pug flotando
│   │   └── Features.tsx       # Grid de 4 features
│   └── admin/
│       ├── AdminSidebar.tsx   # Navegación admin
│       ├── KpiCard.tsx        # Tarjeta de métrica con color-top
│       ├── UploadZone.tsx     # Drag-and-drop PDF uploader
│       ├── DocumentsTable.tsx # Tabla de docs con acciones
│       └── SourcesGrid.tsx    # Estado de 4 fuentes de scraping
├── lib/
│   ├── api.ts                 # fetch wrapper hacia backend, maneja auth token
│   └── types.ts               # Interfaces TS: Message, Conversation, Document, etc.
├── public/
│   └── molly-logo.png         # Logo pug oficial
├── middleware.ts              # Clerk: protege /chat/*, /admin/*, /perfil
├── tailwind.config.ts
├── next.config.ts
└── package.json
```

---

## FASE 1 — Setup del Proyecto

### Task 1: Estructura de repositorio + entorno

**Files:**
- Create: `backend/requirements.txt`
- Create: `backend/.env.example`
- Create: `frontend/package.json` (via scaffold)
- Create: `.gitignore`

- [ ] **Step 1: Inicializar git y estructura de carpetas**

```bash
cd /Users/jm/molly-ia
git init
mkdir -p backend/rag backend/ingest backend/scrapers backend/routers backend/tests backend/migrations
mkdir -p frontend
cp molly-logo.png frontend/public/molly-logo.png 2>/dev/null || true
```

- [ ] **Step 2: Crear .gitignore**

```
# .gitignore
.env
.env.local
__pycache__/
*.pyc
.venv/
node_modules/
.next/
chroma_db/
*.egg-info/
dist/
.DS_Store
```

- [ ] **Step 3: Crear requirements.txt del backend**

```
# backend/requirements.txt
fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pydantic-settings==2.6.1
python-multipart==0.0.12
pymupdf==1.24.14
python-docx==1.1.2
langchain-text-splitters==0.3.2
sentence-transformers==3.3.1
chromadb==0.5.23
groq==0.13.0
boto3==1.35.85
beautifulsoup4==4.12.3
requests==2.32.3
instaloader==4.13.1
google-api-python-client==2.154.0
python-jose[cryptography]==3.3.0
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
```

- [ ] **Step 4: Crear .env.example**

```bash
# backend/.env.example
DATABASE_URL=postgresql://user:password@host:5432/mollyia
CHROMA_PERSIST_DIR=./chroma_db
GROQ_API_KEY=gsk_...
CLERK_SECRET_KEY=sk_live_...
CLERK_PUBLISHABLE_KEY=pk_live_...
R2_ACCOUNT_ID=...
R2_ACCESS_KEY_ID=...
R2_SECRET_ACCESS_KEY=...
R2_BUCKET_NAME=molly-docs
YOUTUBE_API_KEY=...
FACEBOOK_PAGE_ID=colegiogemelli
INSTAGRAM_USERNAME=colegiogemelli
```

- [ ] **Step 5: Instalar dependencias backend**

```bash
cd /Users/jm/molly-ia/backend
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
Expected: instalación sin errores.

- [ ] **Step 6: Scaffold frontend Next.js 14**

```bash
cd /Users/jm/molly-ia
npx create-next-app@14 frontend \
  --typescript --tailwind --eslint \
  --app --src-dir=no \
  --import-alias="@/*" \
  --no-git
```

- [ ] **Step 7: Instalar dependencias frontend**

```bash
cd /Users/jm/molly-ia/frontend
npm install @clerk/nextjs@^5 lucide-react
```

- [ ] **Step 8: Crear .env.local del frontend**

```bash
# frontend/.env.local
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...
CLERK_SECRET_KEY=sk_live_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-in
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat
NEXT_PUBLIC_API_URL=http://localhost:8000
```

- [ ] **Step 9: Commit inicial**

```bash
cd /Users/jm/molly-ia
git add .
git commit -m "feat: scaffold backend + frontend, add dependencies"
```

---

## FASE 2 — Backend Core

### Task 2: Configuración y modelos de base de datos

**Files:**
- Create: `backend/config.py`
- Create: `backend/database.py`
- Create: `backend/models.py`
- Create: `backend/migrations/env.py` (via alembic init)

- [ ] **Step 1: Escribir test de modelos**

```python
# backend/tests/test_models.py
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from backend.models import Base, User, Conversation, Message, Document, ScrapeJob

@pytest.fixture
def db():
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)
    Session = sessionmaker(bind=engine)
    session = Session()
    yield session
    session.close()

def test_create_user(db):
    user = User(clerk_id="user_abc", email="padre@test.com", name="Juan", role="parent")
    db.add(user)
    db.commit()
    assert db.query(User).filter_by(clerk_id="user_abc").first().name == "Juan"

def test_create_conversation_with_messages(db):
    user = User(clerk_id="u1", email="a@b.com", name="A", role="parent")
    db.add(user)
    db.flush()
    conv = Conversation(user_id=user.id, title="Test conv")
    db.add(conv)
    db.flush()
    msg = Message(conversation_id=conv.id, role="user", content="Hola", sources_json="[]")
    db.add(msg)
    db.commit()
    assert len(db.query(Message).filter_by(conversation_id=conv.id).all()) == 1

def test_document_status_default(db):
    doc = Document(name="test.pdf", category="reglamento", source_type="pdf",
                   r2_key="docs/test.pdf", chunks_count=0, status="processing")
    db.add(doc)
    db.commit()
    assert doc.status == "processing"
```

- [ ] **Step 2: Correr test — debe fallar**

```bash
cd /Users/jm/molly-ia/backend
source .venv/bin/activate
pytest tests/test_models.py -v
```
Expected: `ImportError: No module named 'backend.models'`

- [ ] **Step 3: Crear config.py**

```python
# backend/config.py
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    database_url: str
    chroma_persist_dir: str = "./chroma_db"
    groq_api_key: str
    clerk_secret_key: str
    clerk_publishable_key: str
    r2_account_id: str
    r2_access_key_id: str
    r2_secret_access_key: str
    r2_bucket_name: str = "molly-docs"
    youtube_api_key: str = ""
    facebook_page_id: str = "colegiogemelli"
    instagram_username: str = "colegiogemelli"

    class Config:
        env_file = ".env"

settings = Settings()
```

- [ ] **Step 4: Crear database.py**

```python
# backend/database.py
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, DeclarativeBase
from backend.config import settings

engine = create_engine(settings.database_url)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

class Base(DeclarativeBase):
    pass

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

- [ ] **Step 5: Crear models.py**

```python
# backend/models.py
from datetime import datetime
from sqlalchemy import String, Integer, Text, DateTime, ForeignKey
from sqlalchemy.orm import Mapped, mapped_column, relationship
from backend.database import Base

class User(Base):
    __tablename__ = "users"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    clerk_id: Mapped[str] = mapped_column(String(128), unique=True, index=True)
    email: Mapped[str] = mapped_column(String(256), unique=True)
    name: Mapped[str] = mapped_column(String(256))
    role: Mapped[str] = mapped_column(String(32), default="parent")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
    conversations: Mapped[list["Conversation"]] = relationship(back_populates="user")

class Conversation(Base):
    __tablename__ = "conversations"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id"))
    title: Mapped[str] = mapped_column(String(256), default="Nueva conversación")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    user: Mapped["User"] = relationship(back_populates="conversations")
    messages: Mapped[list["Message"]] = relationship(back_populates="conversation", cascade="all, delete")

class Message(Base):
    __tablename__ = "messages"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    conversation_id: Mapped[int] = mapped_column(ForeignKey("conversations.id"))
    role: Mapped[str] = mapped_column(String(16))  # "user" | "assistant"
    content: Mapped[str] = mapped_column(Text)
    sources_json: Mapped[str] = mapped_column(Text, default="[]")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
    conversation: Mapped["Conversation"] = relationship(back_populates="messages")

class Document(Base):
    __tablename__ = "documents"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    name: Mapped[str] = mapped_column(String(512))
    category: Mapped[str] = mapped_column(String(128), default="general")
    source_type: Mapped[str] = mapped_column(String(32))  # pdf | docx | web | facebook | instagram | youtube
    r2_key: Mapped[str] = mapped_column(String(512), default="")
    chunks_count: Mapped[int] = mapped_column(Integer, default=0)
    status: Mapped[str] = mapped_column(String(32), default="processing")  # processing | indexed | error
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)

class ScrapeJob(Base):
    __tablename__ = "scrape_jobs"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    source: Mapped[str] = mapped_column(String(64))  # web | facebook | instagram | youtube
    status: Mapped[str] = mapped_column(String(32))  # running | done | error
    chunks_added: Mapped[int] = mapped_column(Integer, default=0)
    error: Mapped[str] = mapped_column(Text, default="")
    ran_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
```

- [ ] **Step 6: Agregar __init__.py vacíos**

```bash
touch backend/__init__.py backend/rag/__init__.py backend/ingest/__init__.py
touch backend/scrapers/__init__.py backend/routers/__init__.py backend/tests/__init__.py
```

- [ ] **Step 7: Correr tests — deben pasar**

```bash
cd /Users/jm/molly-ia/backend
pytest tests/test_models.py -v
```
Expected: 3 tests PASSED.

- [ ] **Step 8: Inicializar Alembic y primera migración**

```bash
cd /Users/jm/molly-ia/backend
source .venv/bin/activate
alembic init migrations
```

Editar `migrations/env.py` — reemplazar las líneas de `target_metadata`:
```python
# migrations/env.py (solo las líneas que cambian)
from backend.models import Base      # agregar este import
target_metadata = Base.metadata      # reemplazar None por Base.metadata
```

```bash
alembic revision --autogenerate -m "initial schema"
```
Expected: crea `migrations/versions/xxxx_initial_schema.py`

- [ ] **Step 9: Commit**

```bash
cd /Users/jm/molly-ia
git add backend/
git commit -m "feat: database models, config, alembic migrations"
```

---

### Task 3: Pipeline RAG — embeddings + vectorstore + retriever + generator

**Files:**
- Create: `backend/rag/embedder.py`
- Create: `backend/rag/vectorstore.py`
- Create: `backend/rag/retriever.py`
- Create: `backend/rag/generator.py`
- Create: `backend/tests/test_rag.py`

- [ ] **Step 1: Escribir tests del pipeline RAG**

```python
# backend/tests/test_rag.py
import pytest
from unittest.mock import patch, MagicMock
from backend.rag.chunker_helper import split_text
from backend.rag.embedder import Embedder
from backend.rag.vectorstore import VectorStore
from backend.rag.retriever import retrieve_chunks
from backend.rag.generator import build_prompt, SYSTEM_PROMPT

def test_split_text_produces_chunks():
    text = "Hola mundo. " * 200
    chunks = split_text(text)
    assert len(chunks) > 1
    assert all(len(c) > 0 for c in chunks)

def test_embedder_returns_vector():
    embedder = Embedder()
    vec = embedder.embed("Texto de prueba del colegio")
    assert isinstance(vec, list)
    assert len(vec) == 384  # MiniLM-L12-v2 dimension

def test_vectorstore_add_and_query():
    vs = VectorStore(persist_dir="./test_chroma_db")
    vs.add_chunks(
        chunks=["El colegio abre a las 7am", "Las matrículas son en noviembre"],
        metadatas=[{"source": "web"}, {"source": "pdf"}],
        ids=["t1", "t2"]
    )
    results = vs.query("horario de apertura", n_results=1)
    assert len(results) == 1
    assert "7am" in results[0]["text"]
    vs.delete_collection()

def test_build_prompt_includes_context():
    chunks = [{"text": "Matrículas en noviembre", "source": "pdf"}]
    prompt = build_prompt("¿Cuándo son las matrículas?", chunks, history=[])
    assert "noviembre" in prompt
    assert "matrículas" in prompt.lower()

def test_system_prompt_in_spanish():
    assert "Molly" in SYSTEM_PROMPT
    assert "Gemelli" in SYSTEM_PROMPT
    assert "español" in SYSTEM_PROMPT.lower()
```

- [ ] **Step 2: Correr tests — deben fallar**

```bash
pytest backend/tests/test_rag.py -v
```
Expected: `ImportError`

- [ ] **Step 3: Crear embedder.py**

```python
# backend/rag/embedder.py
from sentence_transformers import SentenceTransformer

_model = None

def _get_model():
    global _model
    if _model is None:
        _model = SentenceTransformer("paraphrase-multilingual-MiniLM-L12-v2")
    return _model

class Embedder:
    def embed(self, text: str) -> list[float]:
        return _get_model().encode(text).tolist()

    def embed_batch(self, texts: list[str]) -> list[list[float]]:
        return _get_model().encode(texts).tolist()
```

- [ ] **Step 4: Crear vectorstore.py**

```python
# backend/rag/vectorstore.py
import chromadb
from backend.config import settings

_client = None

def _get_client():
    global _client
    if _client is None:
        _client = chromadb.PersistentClient(path=settings.chroma_persist_dir)
    return _client

COLLECTION_NAME = "molly_docs"

class VectorStore:
    def __init__(self, persist_dir: str | None = None):
        if persist_dir:
            self._client = chromadb.PersistentClient(path=persist_dir)
        else:
            self._client = _get_client()
        self._col = self._client.get_or_create_collection(
            COLLECTION_NAME, metadata={"hnsw:space": "cosine"}
        )

    def add_chunks(self, chunks: list[str], metadatas: list[dict], ids: list[str]):
        from backend.rag.embedder import Embedder
        embeddings = Embedder().embed_batch(chunks)
        self._col.add(documents=chunks, embeddings=embeddings, metadatas=metadatas, ids=ids)

    def query(self, text: str, n_results: int = 5) -> list[dict]:
        from backend.rag.embedder import Embedder
        embedding = Embedder().embed(text)
        results = self._col.query(query_embeddings=[embedding], n_results=n_results)
        return [
            {"text": doc, "source": meta.get("source", ""), "doc_name": meta.get("doc_name", "")}
            for doc, meta in zip(results["documents"][0], results["metadatas"][0])
        ]

    def delete_by_doc_id(self, doc_id: str):
        results = self._col.get(where={"doc_id": doc_id})
        if results["ids"]:
            self._col.delete(ids=results["ids"])

    def delete_collection(self):
        self._client.delete_collection(COLLECTION_NAME)
```

- [ ] **Step 5: Crear chunker helper**

```python
# backend/rag/chunker_helper.py
from langchain_text_splitters import RecursiveCharacterTextSplitter

_splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=50)

def split_text(text: str) -> list[str]:
    return [c.page_content for c in _splitter.create_documents([text])]
```

- [ ] **Step 6: Crear generator.py**

```python
# backend/rag/generator.py
from groq import Groq
from backend.config import settings

SYSTEM_PROMPT = """Eres Molly, la asistente virtual amigable del Colegio Gemelli (Colombia).
Respondes ÚNICAMENTE con información de los documentos oficiales del colegio.
Si no tienes información suficiente, dilo con amabilidad y sugiere contactar secretaría (secretaria@colgemelli.edu.co o llamar al número en la web).
Responde siempre en español, de forma clara y concisa.
No inventes información. Si la pregunta no tiene que ver con el colegio, redirige amablemente."""

def build_prompt(question: str, chunks: list[dict], history: list[dict]) -> str:
    context = "\n\n".join([f"[Fuente: {c['source']}]\n{c['text']}" for c in chunks])
    return f"Contexto del colegio:\n{context}\n\nPregunta del padre: {question}"

def generate_answer(question: str, chunks: list[dict], history: list[dict]) -> str:
    client = Groq(api_key=settings.groq_api_key)
    messages = [{"role": "system", "content": SYSTEM_PROMPT}]
    for h in history[-6:]:  # últimos 3 intercambios
        messages.append({"role": h["role"], "content": h["content"]})
    messages.append({"role": "user", "content": build_prompt(question, chunks, history)})
    response = client.chat.completions.create(
        model="llama-3.3-70b-versatile",
        messages=messages,
        max_tokens=1024,
        temperature=0.3,
    )
    return response.choices[0].message.content

def retrieve_and_generate(question: str, history: list[dict]) -> dict:
    from backend.rag.retriever import retrieve_chunks
    chunks = retrieve_chunks(question)
    answer = generate_answer(question, chunks, history)
    sources = list({c["source"] for c in chunks if c["source"]})
    return {"answer": answer, "sources": sources}
```

- [ ] **Step 7: Crear retriever.py**

```python
# backend/rag/retriever.py
from backend.rag.vectorstore import VectorStore

def retrieve_chunks(question: str, n_results: int = 5) -> list[dict]:
    vs = VectorStore()
    return vs.query(question, n_results=n_results)
```

- [ ] **Step 8: Correr tests RAG**

```bash
cd /Users/jm/molly-ia/backend
pytest tests/test_rag.py -v
```
Expected: 5 tests PASSED. (El test de embedder descarga el modelo ~90MB la primera vez — puede tardar.)

- [ ] **Step 9: Commit**

```bash
cd /Users/jm/molly-ia
git add backend/rag/ backend/tests/test_rag.py
git commit -m "feat: RAG pipeline — embedder, vectorstore, retriever, generator"
```

---

### Task 4: Ingesta de documentos — parsers + R2 storage + chunking

**Files:**
- Create: `backend/ingest/pdf_parser.py`
- Create: `backend/ingest/docx_parser.py`
- Create: `backend/ingest/chunker.py`
- Create: `backend/ingest/storage.py`
- Create: `backend/tests/test_ingest.py`

- [ ] **Step 1: Escribir tests de ingesta**

```python
# backend/tests/test_ingest.py
import pytest
import io
from unittest.mock import patch, MagicMock
from backend.ingest.chunker import chunk_text
from backend.ingest.pdf_parser import extract_text_from_pdf_bytes
from backend.ingest.docx_parser import extract_text_from_docx_bytes

def test_chunk_text_returns_list():
    long_text = "El colegio Gemelli fue fundado. " * 100
    chunks = chunk_text(long_text, doc_id="d1", source="pdf", doc_name="test.pdf")
    assert len(chunks) > 1
    assert all("text" in c and "metadata" in c and "id" in c for c in chunks)
    assert chunks[0]["metadata"]["doc_id"] == "d1"
    assert chunks[0]["metadata"]["source"] == "pdf"

def test_chunk_text_empty_returns_empty():
    assert chunk_text("", doc_id="d1", source="pdf", doc_name="test.pdf") == []

def test_extract_pdf_bytes():
    # Crear PDF mínimo con PyMuPDF
    import fitz
    doc = fitz.open()
    page = doc.new_page()
    page.insert_text((72, 72), "Hola desde el colegio Gemelli")
    pdf_bytes = doc.tobytes()
    text = extract_text_from_pdf_bytes(pdf_bytes)
    assert "Gemelli" in text

def test_extract_docx_bytes():
    from docx import Document
    doc = Document()
    doc.add_paragraph("Reglamento del Colegio Gemelli 2026")
    buf = io.BytesIO()
    doc.save(buf)
    text = extract_text_from_docx_bytes(buf.getvalue())
    assert "Gemelli" in text
```

- [ ] **Step 2: Correr tests — deben fallar**

```bash
pytest backend/tests/test_ingest.py -v
```
Expected: `ImportError`

- [ ] **Step 3: Crear pdf_parser.py**

```python
# backend/ingest/pdf_parser.py
import fitz  # PyMuPDF

def extract_text_from_pdf_bytes(pdf_bytes: bytes) -> str:
    doc = fitz.open(stream=pdf_bytes, filetype="pdf")
    return "\n".join(page.get_text() for page in doc)
```

- [ ] **Step 4: Crear docx_parser.py**

```python
# backend/ingest/docx_parser.py
import io
from docx import Document

def extract_text_from_docx_bytes(docx_bytes: bytes) -> str:
    doc = Document(io.BytesIO(docx_bytes))
    return "\n".join(p.text for p in doc.paragraphs if p.text.strip())
```

- [ ] **Step 5: Crear chunker.py**

```python
# backend/ingest/chunker.py
import hashlib
from backend.rag.chunker_helper import split_text

def chunk_text(text: str, doc_id: str, source: str, doc_name: str) -> list[dict]:
    if not text.strip():
        return []
    raw_chunks = split_text(text)
    result = []
    for i, chunk in enumerate(raw_chunks):
        chunk_id = hashlib.md5(f"{doc_id}_{i}_{chunk[:32]}".encode()).hexdigest()
        result.append({
            "id": chunk_id,
            "text": chunk,
            "metadata": {"doc_id": doc_id, "source": source, "doc_name": doc_name, "chunk_index": i}
        })
    return result
```

- [ ] **Step 6: Crear storage.py**

```python
# backend/ingest/storage.py
import boto3
from backend.config import settings

def _s3_client():
    return boto3.client(
        "s3",
        endpoint_url=f"https://{settings.r2_account_id}.r2.cloudflarestorage.com",
        aws_access_key_id=settings.r2_access_key_id,
        aws_secret_access_key=settings.r2_secret_access_key,
        region_name="auto",
    )

def upload_file(file_bytes: bytes, key: str, content_type: str = "application/pdf") -> str:
    _s3_client().put_object(
        Bucket=settings.r2_bucket_name,
        Key=key,
        Body=file_bytes,
        ContentType=content_type,
    )
    return key

def download_file(key: str) -> bytes:
    response = _s3_client().get_object(Bucket=settings.r2_bucket_name, Key=key)
    return response["Body"].read()

def delete_file(key: str):
    _s3_client().delete_object(Bucket=settings.r2_bucket_name, Key=key)
```

- [ ] **Step 7: Correr tests**

```bash
pytest backend/tests/test_ingest.py -v
```
Expected: 4 tests PASSED.

- [ ] **Step 8: Commit**

```bash
git add backend/ingest/ backend/tests/test_ingest.py
git commit -m "feat: document ingestion — pdf/docx parsers, chunker, R2 storage"
```

---

### Task 5: Auth middleware + schemas + main app + routers

**Files:**
- Create: `backend/auth.py`
- Create: `backend/schemas.py`
- Create: `backend/main.py`
- Create: `backend/routers/chat.py`
- Create: `backend/routers/documents.py`
- Create: `backend/routers/scrape.py`
- Create: `backend/routers/stats.py`
- Create: `backend/tests/conftest.py`
- Create: `backend/tests/test_routers.py`

- [ ] **Step 1: Crear schemas.py**

```python
# backend/schemas.py
from pydantic import BaseModel
from datetime import datetime

class ChatRequest(BaseModel):
    conversation_id: int | None = None
    message: str

class ChatResponse(BaseModel):
    conversation_id: int
    message_id: int
    answer: str
    sources: list[str]

class ConversationOut(BaseModel):
    id: int
    title: str
    updated_at: datetime
    class Config: from_attributes = True

class MessageOut(BaseModel):
    id: int
    role: str
    content: str
    sources_json: str
    created_at: datetime
    class Config: from_attributes = True

class DocumentOut(BaseModel):
    id: int
    name: str
    category: str
    source_type: str
    chunks_count: int
    status: str
    created_at: datetime
    class Config: from_attributes = True

class ScrapeJobOut(BaseModel):
    id: int
    source: str
    status: str
    chunks_added: int
    error: str
    ran_at: datetime
    class Config: from_attributes = True

class StatsOut(BaseModel):
    docs_indexed: int
    questions_answered: int
    active_users: int
    scrape_jobs: int
```

- [ ] **Step 2: Crear auth.py**

```python
# backend/auth.py
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
import httpx
from jose import jwt, JWTError
from backend.config import settings

bearer = HTTPBearer()

async def get_clerk_public_keys() -> dict:
    url = f"https://api.clerk.com/v1/jwks"
    async with httpx.AsyncClient() as client:
        resp = await client.get(url, headers={"Authorization": f"Bearer {settings.clerk_secret_key}"})
        return resp.json()

async def get_current_user(credentials: HTTPAuthorizationCredentials = Depends(bearer)) -> dict:
    token = credentials.credentials
    try:
        header = jwt.get_unverified_header(token)
        jwks = await get_clerk_public_keys()
        key = next((k for k in jwks["keys"] if k["kid"] == header["kid"]), None)
        if not key:
            raise HTTPException(status_code=401, detail="Invalid token key")
        payload = jwt.decode(token, key, algorithms=["RS256"])
        return {
            "clerk_id": payload["sub"],
            "role": payload.get("public_metadata", {}).get("role", "parent"),
        }
    except JWTError:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")

async def require_admin(user: dict = Depends(get_current_user)) -> dict:
    if user["role"] != "admin":
        raise HTTPException(status_code=403, detail="Admin only")
    return user
```

- [ ] **Step 3: Crear routers/chat.py**

```python
# backend/routers/chat.py
import json
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from backend.database import get_db
from backend.auth import get_current_user
from backend.models import User, Conversation, Message
from backend.schemas import ChatRequest, ChatResponse, ConversationOut, MessageOut
from backend.rag.generator import retrieve_and_generate

router = APIRouter(prefix="/chat", tags=["chat"])

def _get_or_create_user(db: Session, clerk_id: str) -> User:
    user = db.query(User).filter_by(clerk_id=clerk_id).first()
    if not user:
        user = User(clerk_id=clerk_id, email="", name="", role="parent")
        db.add(user)
        db.commit()
        db.refresh(user)
    return user

@router.post("", response_model=ChatResponse)
async def chat(req: ChatRequest, db: Session = Depends(get_db), user_data: dict = Depends(get_current_user)):
    user = _get_or_create_user(db, user_data["clerk_id"])
    if req.conversation_id:
        conv = db.query(Conversation).filter_by(id=req.conversation_id, user_id=user.id).first()
    else:
        conv = Conversation(user_id=user.id, title=req.message[:60])
        db.add(conv)
        db.commit()
        db.refresh(conv)
    history = [{"role": m.role, "content": m.content}
               for m in db.query(Message).filter_by(conversation_id=conv.id).order_by(Message.id).all()]
    result = retrieve_and_generate(req.message, history)
    user_msg = Message(conversation_id=conv.id, role="user", content=req.message, sources_json="[]")
    db.add(user_msg)
    db.flush()
    assistant_msg = Message(
        conversation_id=conv.id, role="assistant",
        content=result["answer"], sources_json=json.dumps(result["sources"])
    )
    db.add(assistant_msg)
    db.commit()
    db.refresh(assistant_msg)
    return ChatResponse(conversation_id=conv.id, message_id=assistant_msg.id,
                        answer=result["answer"], sources=result["sources"])

@router.get("/conversations", response_model=list[ConversationOut])
async def list_conversations(db: Session = Depends(get_db), user_data: dict = Depends(get_current_user)):
    user = _get_or_create_user(db, user_data["clerk_id"])
    return db.query(Conversation).filter_by(user_id=user.id).order_by(Conversation.updated_at.desc()).all()

@router.get("/conversations/{conv_id}/messages", response_model=list[MessageOut])
async def get_messages(conv_id: int, db: Session = Depends(get_db), user_data: dict = Depends(get_current_user)):
    user = _get_or_create_user(db, user_data["clerk_id"])
    conv = db.query(Conversation).filter_by(id=conv_id, user_id=user.id).first()
    if not conv:
        from fastapi import HTTPException
        raise HTTPException(404, "Conversation not found")
    return db.query(Message).filter_by(conversation_id=conv_id).order_by(Message.id).all()
```

- [ ] **Step 4: Crear routers/documents.py**

```python
# backend/routers/documents.py
import uuid
from fastapi import APIRouter, Depends, UploadFile, File, Form, HTTPException
from sqlalchemy.orm import Session
from backend.database import get_db
from backend.auth import require_admin
from backend.models import Document
from backend.schemas import DocumentOut
from backend.ingest.storage import upload_file, delete_file
from backend.ingest.pdf_parser import extract_text_from_pdf_bytes
from backend.ingest.docx_parser import extract_text_from_docx_bytes
from backend.ingest.chunker import chunk_text
from backend.rag.vectorstore import VectorStore

router = APIRouter(prefix="/documents", tags=["documents"])

@router.get("", response_model=list[DocumentOut])
async def list_documents(db: Session = Depends(get_db), _=Depends(require_admin)):
    return db.query(Document).order_by(Document.created_at.desc()).all()

@router.post("", response_model=DocumentOut)
async def upload_document(
    file: UploadFile = File(...),
    category: str = Form("general"),
    db: Session = Depends(get_db),
    _=Depends(require_admin)
):
    file_bytes = await file.read()
    filename = file.filename or "documento"
    ext = filename.rsplit(".", 1)[-1].lower()
    if ext not in ("pdf", "docx", "txt"):
        raise HTTPException(400, "Solo se aceptan PDF, DOCX o TXT")
    r2_key = f"docs/{uuid.uuid4()}/{filename}"
    upload_file(file_bytes, r2_key, content_type=file.content_type or "application/octet-stream")
    doc = Document(name=filename, category=category, source_type=ext, r2_key=r2_key, status="processing")
    db.add(doc)
    db.commit()
    db.refresh(doc)
    if ext == "pdf":
        text = extract_text_from_pdf_bytes(file_bytes)
    elif ext == "docx":
        text = extract_text_from_docx_bytes(file_bytes)
    else:
        text = file_bytes.decode("utf-8", errors="ignore")
    chunks = chunk_text(text, doc_id=str(doc.id), source="pdf", doc_name=filename)
    if chunks:
        vs = VectorStore()
        vs.add_chunks(
            chunks=[c["text"] for c in chunks],
            metadatas=[c["metadata"] for c in chunks],
            ids=[c["id"] for c in chunks]
        )
    doc.chunks_count = len(chunks)
    doc.status = "indexed" if chunks else "error"
    db.commit()
    db.refresh(doc)
    return doc

@router.delete("/{doc_id}")
async def delete_document(doc_id: int, db: Session = Depends(get_db), _=Depends(require_admin)):
    doc = db.query(Document).filter_by(id=doc_id).first()
    if not doc:
        raise HTTPException(404, "Document not found")
    if doc.r2_key:
        try:
            delete_file(doc.r2_key)
        except Exception:
            pass
    VectorStore().delete_by_doc_id(str(doc_id))
    db.delete(doc)
    db.commit()
    return {"ok": True}
```

- [ ] **Step 5: Crear routers/scrape.py**

```python
# backend/routers/scrape.py
from fastapi import APIRouter, Depends, BackgroundTasks
from sqlalchemy.orm import Session
from backend.database import get_db
from backend.auth import require_admin
from backend.models import ScrapeJob
from backend.schemas import ScrapeJobOut

router = APIRouter(prefix="/scrape", tags=["scrape"])

SOURCES = ["web", "facebook", "instagram", "youtube"]

def run_scraper(source: str, db: Session):
    job = ScrapeJob(source=source, status="running")
    db.add(job)
    db.commit()
    db.refresh(job)
    try:
        if source == "web":
            from backend.scrapers.web_scraper import scrape_website
            chunks_added = scrape_website()
        elif source == "facebook":
            from backend.scrapers.facebook_scraper import scrape_facebook
            chunks_added = scrape_facebook()
        elif source == "instagram":
            from backend.scrapers.instagram_scraper import scrape_instagram
            chunks_added = scrape_instagram()
        elif source == "youtube":
            from backend.scrapers.youtube_scraper import scrape_youtube
            chunks_added = scrape_youtube()
        else:
            chunks_added = 0
        job.status = "done"
        job.chunks_added = chunks_added
    except Exception as e:
        job.status = "error"
        job.error = str(e)
    db.commit()

@router.post("/{source}")
async def trigger_scrape(
    source: str, background_tasks: BackgroundTasks,
    db: Session = Depends(get_db), _=Depends(require_admin)
):
    if source not in SOURCES:
        from fastapi import HTTPException
        raise HTTPException(400, f"Source must be one of {SOURCES}")
    background_tasks.add_task(run_scraper, source, db)
    return {"ok": True, "message": f"Scraping {source} started in background"}

@router.get("/jobs", response_model=list[ScrapeJobOut])
async def list_jobs(db: Session = Depends(get_db), _=Depends(require_admin)):
    return db.query(ScrapeJob).order_by(ScrapeJob.ran_at.desc()).limit(20).all()
```

- [ ] **Step 6: Crear routers/stats.py**

```python
# backend/routers/stats.py
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from sqlalchemy import func
from backend.database import get_db
from backend.auth import require_admin
from backend.models import Document, Message, User, ScrapeJob
from backend.schemas import StatsOut

router = APIRouter(prefix="/stats", tags=["stats"])

@router.get("", response_model=StatsOut)
async def get_stats(db: Session = Depends(get_db), _=Depends(require_admin)):
    docs_indexed = db.query(func.count(Document.id)).filter_by(status="indexed").scalar()
    questions_answered = db.query(func.count(Message.id)).filter_by(role="assistant").scalar()
    active_users = db.query(func.count(User.id)).scalar()
    scrape_jobs = db.query(func.count(ScrapeJob.id)).scalar()
    return StatsOut(
        docs_indexed=docs_indexed,
        questions_answered=questions_answered,
        active_users=active_users,
        scrape_jobs=scrape_jobs
    )
```

- [ ] **Step 7: Crear main.py**

```python
# backend/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from backend.routers import chat, documents, scrape, stats

app = FastAPI(title="Molly IA API", version="1.0.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "https://*.vercel.app"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(chat.router)
app.include_router(documents.router)
app.include_router(scrape.router)
app.include_router(stats.router)

@app.get("/health")
def health():
    return {"status": "ok", "service": "Molly IA Backend"}
```

- [ ] **Step 8: Crear conftest.py + test básico de health**

```python
# backend/tests/conftest.py
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from backend.database import Base, get_db
from backend.main import app

TEST_DB_URL = "sqlite:///./test.db"
engine = create_engine(TEST_DB_URL, connect_args={"check_same_thread": False})
TestingSession = sessionmaker(bind=engine)

@pytest.fixture(scope="session", autouse=True)
def setup_db():
    Base.metadata.create_all(engine)
    yield
    Base.metadata.drop_all(engine)

@pytest.fixture
def db():
    session = TestingSession()
    yield session
    session.close()

@pytest.fixture
def client(db):
    def override_db():
        yield db
    app.dependency_overrides[get_db] = override_db
    with TestClient(app) as c:
        yield c
    app.dependency_overrides.clear()
```

```python
# backend/tests/test_routers.py
def test_health(client):
    r = client.get("/health")
    assert r.status_code == 200
    assert r.json()["status"] == "ok"
```

- [ ] **Step 9: Correr test de health**

```bash
cd /Users/jm/molly-ia/backend
pytest tests/test_routers.py::test_health -v
```
Expected: PASSED.

- [ ] **Step 10: Arrancar el servidor en local**

```bash
cd /Users/jm/molly-ia/backend
cp .env.example .env  # completar con tus keys reales
uvicorn backend.main:app --reload --port 8000
```
Expected: `Uvicorn running on http://127.0.0.1:8000`. Abrir `http://localhost:8000/health` → `{"status":"ok"}`.

- [ ] **Step 11: Commit**

```bash
cd /Users/jm/molly-ia
git add backend/
git commit -m "feat: FastAPI app — auth, chat, documents, scrape, stats routers"
```

---

## FASE 3 — Scrapers

### Task 6: Scrapers web + redes sociales

**Files:**
- Create: `backend/scrapers/web_scraper.py`
- Create: `backend/scrapers/facebook_scraper.py`
- Create: `backend/scrapers/instagram_scraper.py`
- Create: `backend/scrapers/youtube_scraper.py`
- Create: `backend/tests/test_scrapers.py`

- [ ] **Step 1: Escribir tests de scrapers (con mocks)**

```python
# backend/tests/test_scrapers.py
from unittest.mock import patch, MagicMock

def test_web_scraper_returns_int(mocker):
    mocker.patch("requests.get", return_value=MagicMock(
        status_code=200,
        text="<html><body><p>El colegio Gemelli abre en enero</p></body></html>",
        content=b"<html><body><p>El colegio Gemelli abre en enero</p></body></html>"
    ))
    mocker.patch("backend.scrapers.web_scraper._index_chunks", return_value=3)
    from backend.scrapers.web_scraper import scrape_website
    result = scrape_website()
    assert isinstance(result, int)

def test_youtube_scraper_returns_int(mocker):
    mock_service = MagicMock()
    mock_service.search().list().execute.return_value = {
        "items": [{"id": {"videoId": "abc123"}, "snippet": {
            "title": "Video del colegio", "description": "Evento de grado 2025"
        }}]
    }
    mocker.patch("backend.scrapers.youtube_scraper._get_youtube_service", return_value=mock_service)
    mocker.patch("backend.scrapers.youtube_scraper._index_chunks", return_value=2)
    from backend.scrapers.youtube_scraper import scrape_youtube
    result = scrape_youtube()
    assert isinstance(result, int)
```

- [ ] **Step 2: Correr tests — deben fallar**

```bash
pytest backend/tests/test_scrapers.py -v
```
Expected: ImportError

- [ ] **Step 3: Crear web_scraper.py**

```python
# backend/scrapers/web_scraper.py
import requests
from bs4 import BeautifulSoup
from urllib.parse import urljoin, urlparse
from backend.ingest.chunker import chunk_text
from backend.rag.vectorstore import VectorStore
from backend.config import settings

BASE_URL = "https://www.colgemelli.edu.co"
MAX_PAGES = 50

def _get_links(url: str, visited: set) -> list[str]:
    try:
        resp = requests.get(url, timeout=10, headers={"User-Agent": "MollyBot/1.0"})
        if resp.status_code != 200:
            return []
        soup = BeautifulSoup(resp.text, "html.parser")
        links = []
        for a in soup.find_all("a", href=True):
            href = urljoin(url, a["href"])
            parsed = urlparse(href)
            if parsed.netloc == urlparse(BASE_URL).netloc and href not in visited:
                links.append(href)
        return links
    except Exception:
        return []

def _extract_text(url: str) -> str:
    try:
        resp = requests.get(url, timeout=10, headers={"User-Agent": "MollyBot/1.0"})
        soup = BeautifulSoup(resp.text, "html.parser")
        for tag in soup(["script", "style", "nav", "footer", "header"]):
            tag.decompose()
        return soup.get_text(separator="\n", strip=True)
    except Exception:
        return ""

def _index_chunks(url: str, text: str) -> int:
    chunks = chunk_text(text, doc_id=f"web_{url}", source="web", doc_name=url)
    if chunks:
        vs = VectorStore()
        vs.add_chunks([c["text"] for c in chunks], [c["metadata"] for c in chunks], [c["id"] for c in chunks])
    return len(chunks)

def scrape_website() -> int:
    visited = set()
    to_visit = [BASE_URL]
    total = 0
    while to_visit and len(visited) < MAX_PAGES:
        url = to_visit.pop(0)
        if url in visited:
            continue
        visited.add(url)
        text = _extract_text(url)
        if text:
            total += _index_chunks(url, text)
        to_visit.extend(_get_links(url, visited))
    return total
```

- [ ] **Step 4: Crear facebook_scraper.py**

```python
# backend/scrapers/facebook_scraper.py
import requests
from bs4 import BeautifulSoup
from backend.ingest.chunker import chunk_text
from backend.rag.vectorstore import VectorStore
from backend.config import settings

FB_URL = f"https://www.facebook.com/{settings.facebook_page_id}/posts"

def scrape_facebook() -> int:
    headers = {
        "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
        "Accept-Language": "es-CO,es;q=0.9",
    }
    try:
        resp = requests.get(FB_URL, headers=headers, timeout=15)
        soup = BeautifulSoup(resp.text, "html.parser")
        posts = []
        for div in soup.find_all("div", {"data-testid": "post_message"}):
            text = div.get_text(separator=" ", strip=True)
            if text:
                posts.append(text)
        if not posts:
            # Fallback: extract all paragraph text
            posts = [p.get_text(strip=True) for p in soup.find_all("p") if len(p.get_text(strip=True)) > 40]
        combined = "\n\n".join(posts[:30])
        if not combined:
            return 0
        chunks = chunk_text(combined, doc_id="facebook_posts", source="facebook", doc_name="Facebook Colegio Gemelli")
        if chunks:
            vs = VectorStore()
            vs.add_chunks([c["text"] for c in chunks], [c["metadata"] for c in chunks], [c["id"] for c in chunks])
        return len(chunks)
    except Exception as e:
        print(f"Facebook scraper error: {e}")
        return 0
```

- [ ] **Step 5: Crear instagram_scraper.py**

```python
# backend/scrapers/instagram_scraper.py
import instaloader
from backend.ingest.chunker import chunk_text
from backend.rag.vectorstore import VectorStore
from backend.config import settings

def scrape_instagram() -> int:
    L = instaloader.Instaloader()
    try:
        profile = instaloader.Profile.from_username(L.context, settings.instagram_username)
        captions = []
        for i, post in enumerate(profile.get_posts()):
            if i >= 30:
                break
            if post.caption:
                captions.append(f"[Instagram {post.date.strftime('%Y-%m-%d')}] {post.caption}")
        combined = "\n\n".join(captions)
        if not combined:
            return 0
        chunks = chunk_text(combined, doc_id="instagram_posts", source="instagram", doc_name="Instagram Colegio Gemelli")
        if chunks:
            vs = VectorStore()
            vs.add_chunks([c["text"] for c in chunks], [c["metadata"] for c in chunks], [c["id"] for c in chunks])
        return len(chunks)
    except Exception as e:
        print(f"Instagram scraper error: {e}")
        return 0
```

- [ ] **Step 6: Crear youtube_scraper.py**

```python
# backend/scrapers/youtube_scraper.py
from googleapiclient.discovery import build
from backend.ingest.chunker import chunk_text
from backend.rag.vectorstore import VectorStore
from backend.config import settings

CHANNEL_QUERY = "Colegio Gemelli"

def _get_youtube_service():
    return build("youtube", "v3", developerKey=settings.youtube_api_key)

def _index_chunks(text: str, source_name: str) -> int:
    chunks = chunk_text(text, doc_id="youtube_videos", source="youtube", doc_name=source_name)
    if chunks:
        vs = VectorStore()
        vs.add_chunks([c["text"] for c in chunks], [c["metadata"] for c in chunks], [c["id"] for c in chunks])
    return len(chunks)

def scrape_youtube() -> int:
    youtube = _get_youtube_service()
    request = youtube.search().list(q=CHANNEL_QUERY, part="snippet", type="video", maxResults=25)
    response = request.execute()
    entries = []
    for item in response.get("items", []):
        snippet = item["snippet"]
        entries.append(f"[YouTube] {snippet['title']}\n{snippet['description']}")
    combined = "\n\n".join(entries)
    if not combined:
        return 0
    return _index_chunks(combined, "YouTube Colegio Gemelli")
```

- [ ] **Step 7: Correr tests de scrapers**

```bash
pytest backend/tests/test_scrapers.py -v
```
Expected: 2 tests PASSED.

- [ ] **Step 8: Commit**

```bash
git add backend/scrapers/ backend/tests/test_scrapers.py
git commit -m "feat: scrapers — web, facebook, instagram, youtube"
```

---

## FASE 4 — Frontend: Auth + Diseño base

### Task 7: Configuración global — Tailwind, fuentes, CSS vars, Clerk, middleware

**Files:**
- Modify: `frontend/app/globals.css`
- Modify: `frontend/app/layout.tsx`
- Create: `frontend/middleware.ts`
- Create: `frontend/tailwind.config.ts`
- Create: `frontend/components/ui/MollyAvatar.tsx`
- Create: `frontend/lib/types.ts`
- Create: `frontend/lib/api.ts`

- [ ] **Step 1: Configurar tailwind.config.ts**

```typescript
// frontend/tailwind.config.ts
import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        navy:    { DEFAULT: "#0f1e3c", mid: "#162445", lt: "#1e3260" },
        gold:    { DEFAULT: "#c9922a", lt: "#e8b455" },
        emerald: { DEFAULT: "#1e5c40", lt: "#2d8a5e" },
        cream:   { DEFAULT: "#faf7f0", d: "#ede5d0" },
        sand:    "#d6c9a8",
        muted:   "#6b5f4a",
      },
      fontFamily: {
        display: ["Cormorant Garamond", "serif"],
        sans:    ["Nunito", "sans-serif"],
      },
    },
  },
  plugins: [],
};
export default config;
```

- [ ] **Step 2: Configurar globals.css**

```css
/* frontend/app/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,600;0,700;1,600&family=Nunito:wght@300;400;500;600;700;800&display=swap');

:root {
  --navy: #0f1e3c;
  --gold: #c9922a;
  --gold-lt: #e8b455;
  --emerald: #1e5c40;
  --cream: #faf7f0;
}

* { box-sizing: border-box; }
body { font-family: 'Nunito', sans-serif; background: var(--cream); }

@keyframes float {
  0%, 100% { transform: translateY(0) rotate(-1.5deg); }
  50%       { transform: translateY(-9px) rotate(1.5deg); }
}
@keyframes molly-glow {
  0%, 100% { filter: drop-shadow(0 6px 18px rgba(201,146,42,.35)); }
  50%       { filter: drop-shadow(0 10px 32px rgba(201,146,42,.7)); }
}
.molly-float { animation: float 3.5s ease-in-out infinite, molly-glow 3.5s ease-in-out infinite; }
```

- [ ] **Step 3: Configurar root layout con Clerk**

```typescript
// frontend/app/layout.tsx
import { ClerkProvider } from "@clerk/nextjs";
import "./globals.css";

export const metadata = { title: "Molly IA · Colegio Gemelli", description: "Tu asistente escolar" };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <ClerkProvider>
      <html lang="es">
        <body>{children}</body>
      </html>
    </ClerkProvider>
  );
}
```

- [ ] **Step 4: Crear middleware.ts**

```typescript
// frontend/middleware.ts
import { clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server";

const isProtected = createRouteMatcher(["/chat(.*)", "/admin(.*)", "/perfil(.*)"]);
const isAdmin = createRouteMatcher(["/admin(.*)"]);

export default clerkMiddleware(async (auth, req) => {
  if (isProtected(req)) await auth.protect();
  if (isAdmin(req)) {
    const { sessionClaims } = await auth();
    const role = (sessionClaims?.publicMetadata as any)?.role;
    if (role !== "admin") {
      const { redirectToSignIn } = await auth();
      return redirectToSignIn();
    }
  }
});

export const config = { matcher: ["/((?!_next|.*\\..*).*)"] };
```

- [ ] **Step 5: Crear lib/types.ts**

```typescript
// frontend/lib/types.ts
export interface Conversation { id: number; title: string; updated_at: string; }
export interface Message { id: number; role: "user" | "assistant"; content: string; sources_json: string; created_at: string; }
export interface Document { id: number; name: string; category: string; source_type: string; chunks_count: number; status: "processing" | "indexed" | "error"; created_at: string; }
export interface ScrapeJob { id: number; source: string; status: string; chunks_added: number; error: string; ran_at: string; }
export interface Stats { docs_indexed: number; questions_answered: number; active_users: number; scrape_jobs: number; }
export interface ChatResponse { conversation_id: number; message_id: number; answer: string; sources: string[]; }
```

- [ ] **Step 6: Crear lib/api.ts**

```typescript
// frontend/lib/api.ts
import { useAuth } from "@clerk/nextjs";

const BASE = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000";

async function apiFetch(path: string, token: string, opts: RequestInit = {}) {
  const res = await fetch(`${BASE}${path}`, {
    ...opts,
    headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}`, ...opts.headers },
  });
  if (!res.ok) throw new Error(`API ${path} → ${res.status}`);
  return res.json();
}

export function useApi() {
  const { getToken } = useAuth();
  const token = async () => (await getToken()) ?? "";

  return {
    sendMessage: async (message: string, conversation_id?: number) =>
      apiFetch("/chat", await token(), { method: "POST", body: JSON.stringify({ message, conversation_id }) }),
    getConversations: async () => apiFetch("/chat/conversations", await token()),
    getMessages: async (id: number) => apiFetch(`/chat/conversations/${id}/messages`, await token()),
    getDocuments: async () => apiFetch("/documents", await token()),
    deleteDocument: async (id: number) => apiFetch(`/documents/${id}`, await token(), { method: "DELETE" }),
    triggerScrape: async (source: string) => apiFetch(`/scrape/${source}`, await token(), { method: "POST" }),
    getScrapeJobs: async () => apiFetch("/scrape/jobs", await token()),
    getStats: async () => apiFetch("/stats", await token()),
  };
}
```

- [ ] **Step 7: Crear MollyAvatar.tsx**

```typescript
// frontend/components/ui/MollyAvatar.tsx
import Image from "next/image";

interface Props { size?: number; float?: boolean; className?: string; }

export default function MollyAvatar({ size = 40, float = false, className = "" }: Props) {
  return (
    <div
      className={`rounded-full bg-navy flex items-center justify-center overflow-hidden flex-shrink-0 border-2 border-gold/40 ${float ? "molly-float" : ""} ${className}`}
      style={{ width: size, height: size }}
    >
      <Image src="/molly-logo.png" alt="Molly IA" width={size - 8} height={size - 8} style={{ objectFit: "contain" }} />
    </div>
  );
}
```

- [ ] **Step 8: Arrancar dev server y verificar**

```bash
cd /Users/jm/molly-ia/frontend
npm run dev
```
Abrir `http://localhost:3000` — debe cargar sin errores en consola.

- [ ] **Step 9: Commit**

```bash
cd /Users/jm/molly-ia
git add frontend/
git commit -m "feat: frontend base — Tailwind, Clerk, middleware, MollyAvatar, API client"
```

---

### Task 8: Landing page

**Files:**
- Modify: `frontend/app/page.tsx`
- Create: `frontend/components/landing/Hero.tsx`
- Create: `frontend/components/landing/Features.tsx`

- [ ] **Step 1: Crear Hero.tsx**

```typescript
// frontend/components/landing/Hero.tsx
"use client";
import { useRouter } from "next/navigation";
import MollyAvatar from "@/components/ui/MollyAvatar";

export default function Hero() {
  const router = useRouter();
  return (
    <section className="min-h-screen bg-navy relative overflow-hidden flex items-center">
      {/* Tile background */}
      <div className="absolute inset-0 opacity-[0.04]" style={{
        backgroundImage: "repeating-linear-gradient(45deg,#c9922a 0,#c9922a 1px,transparent 0,transparent 50%),repeating-linear-gradient(-45deg,#c9922a 0,#c9922a 1px,transparent 0,transparent 50%)",
        backgroundSize: "38px 38px"
      }} />
      {/* Glow orbs */}
      <div className="absolute -top-40 -right-20 w-[600px] h-[600px] rounded-full" style={{background:"radial-gradient(circle,rgba(201,146,42,.1) 0%,transparent 70%)"}} />
      <div className="absolute -bottom-20 -left-24 w-[450px] h-[450px] rounded-full" style={{background:"radial-gradient(circle,rgba(30,92,64,.16) 0%,transparent 70%)"}} />

      <div className="relative z-10 grid grid-cols-2 gap-16 items-center px-18 py-16 max-w-7xl mx-auto w-full" style={{padding:"72px"}}>
        {/* Left */}
        <div>
          <div className="inline-flex items-center gap-3 bg-gold/10 border border-gold/30 rounded-full px-5 py-2 mb-8">
            <span className="w-2 h-2 rounded-full bg-gold animate-pulse" />
            <span className="text-[11px] font-bold text-gold-lt tracking-widest uppercase">Colegio Gemelli — Sistema IA Educativo</span>
          </div>
          <h1 className="font-display text-7xl font-light text-white leading-[1.02] mb-2">
            Hola,<br/>soy <em className="text-gold font-semibold not-italic">Molly</em>
          </h1>
          <p className="font-display text-2xl font-light text-sand italic mb-6">Tu asistente del Colegio Gemelli</p>
          <p className="text-white/60 text-[15px] leading-relaxed max-w-md mb-10 font-light">
            Resuelve dudas sobre matrículas, eventos, circulares y horarios — al instante, disponible 24/7, siempre con información oficial.
          </p>
          <div className="flex gap-4">
            <button onClick={() => router.push("/chat")}
              className="bg-gold text-navy px-9 py-4 rounded-full text-sm font-black tracking-tight hover:bg-gold-lt hover:-translate-y-0.5 hover:shadow-[0_8px_24px_rgba(201,146,42,.45)] transition-all">
              Hablar con Molly →
            </button>
            <button onClick={() => router.push("/sign-in")}
              className="border border-sand/30 text-sand px-7 py-4 rounded-full text-sm font-semibold hover:border-sand hover:text-white transition-all">
              Iniciar sesión
            </button>
          </div>
          {/* Ornament */}
          <div className="flex items-center gap-4 my-10">
            <div className="flex-1 h-px bg-gradient-to-r from-transparent via-gold/40 to-transparent" />
            <div className="w-2 h-2 bg-gold rotate-45" />
            <div className="flex-1 h-px bg-gradient-to-r from-transparent via-gold/40 to-transparent" />
          </div>
          {/* Stats */}
          <div className="grid grid-cols-3 gap-px bg-gold/15 border border-gold/15 rounded-2xl overflow-hidden">
            {[["24/7","Disponible"],["<1s","Respuesta"],["5+","Fuentes"]].map(([n,l]) => (
              <div key={l} className="bg-white/[.03] py-5 text-center">
                <div className="font-display text-4xl font-semibold text-gold">{n}</div>
                <div className="text-[10px] text-white/40 tracking-widest uppercase mt-1">{l}</div>
              </div>
            ))}
          </div>
        </div>
        {/* Right: pug floating */}
        <div className="flex justify-center">
          <MollyAvatar size={240} float className="!border-0 !bg-transparent !rounded-none !overflow-visible" />
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Crear Features.tsx**

```typescript
// frontend/components/landing/Features.tsx
const FEATURES = [
  { icon: "📄", title: "Documentos oficiales", desc: "Circulares, reglamentos y PDFs del colegio en tiempo real" },
  { icon: "📱", title: "Redes sociales", desc: "Conectada a Facebook, Instagram y YouTube del colegio" },
  { icon: "🌐", title: "Sitio web actualizado", desc: "Scraping semanal de colgemelli.edu.co" },
  { icon: "🔒", title: "Solo para familias", desc: "Acceso con Google o usuario/contraseña. Privado y seguro." },
];

export default function Features() {
  return (
    <section className="bg-cream py-14 px-18 border-t border-cream-d" style={{padding:"56px 72px"}}>
      <h2 className="font-display text-5xl font-light text-navy text-center mb-2">
        ¿Por qué <em className="text-gold">Molly</em> es diferente?
      </h2>
      <p className="text-center text-muted text-sm mb-12 font-light">Información real del colegio, no respuestas genéricas</p>
      <div className="grid grid-cols-4 gap-5 max-w-5xl mx-auto">
        {FEATURES.map(f => (
          <div key={f.title} className="bg-white border border-cream-d rounded-2xl p-6 group hover:-translate-y-1 hover:shadow-lg hover:border-sand transition-all relative overflow-hidden">
            <div className="absolute top-0 left-0 right-0 h-[3px] bg-gradient-to-r from-gold to-gold-lt scale-x-0 group-hover:scale-x-100 transition-transform origin-left" />
            <div className="text-3xl mb-3">{f.icon}</div>
            <div className="font-display text-xl font-semibold text-navy mb-2">{f.title}</div>
            <p className="text-muted text-xs leading-relaxed font-light">{f.desc}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Actualizar page.tsx**

```typescript
// frontend/app/page.tsx
import Hero from "@/components/landing/Hero";
import Features from "@/components/landing/Features";

export default function Home() {
  return (
    <>
      <Hero />
      <Features />
    </>
  );
}
```

- [ ] **Step 4: Verificar en browser**

```bash
# Dev server ya corriendo en localhost:3000
# Verificar: landing carga, pug visible, botones funcionan
```
Expected: Landing page con pug, colores navy/oro, sin errores de consola.

- [ ] **Step 5: Commit**

```bash
git add frontend/app/page.tsx frontend/components/landing/
git commit -m "feat: landing page — hero with floating Molly, features grid"
```

---

### Task 9: Página de sign-in + chat layout + sidebar

**Files:**
- Create: `frontend/app/(auth)/sign-in/[[...sign-in]]/page.tsx`
- Create: `frontend/app/chat/layout.tsx`
- Create: `frontend/components/chat/ChatSidebar.tsx`

- [ ] **Step 1: Crear sign-in page**

```typescript
// frontend/app/(auth)/sign-in/[[...sign-in]]/page.tsx
import { SignIn } from "@clerk/nextjs";

export default function SignInPage() {
  return (
    <div className="min-h-screen bg-navy flex items-center justify-center">
      <SignIn />
    </div>
  );
}
```

- [ ] **Step 2: Crear ChatSidebar.tsx**

```typescript
// frontend/components/chat/ChatSidebar.tsx
"use client";
import { useEffect, useState } from "react";
import { useRouter, useParams } from "next/navigation";
import { useUser } from "@clerk/nextjs";
import MollyAvatar from "@/components/ui/MollyAvatar";
import { useApi } from "@/lib/api";
import type { Conversation } from "@/lib/types";

export default function ChatSidebar() {
  const router = useRouter();
  const params = useParams();
  const { user } = useUser();
  const api = useApi();
  const [convs, setConvs] = useState<Conversation[]>([]);

  useEffect(() => {
    api.getConversations().then(setConvs).catch(console.error);
  }, []);

  const activeId = params?.id ? Number(params.id) : null;

  return (
    <aside className="w-[272px] bg-navy flex flex-col border-r border-gold/[.13]">
      <div className="p-5 border-b border-white/[.05]">
        <div className="flex items-center gap-2.5 mb-4">
          <MollyAvatar size={34} />
          <div>
            <div className="font-display text-lg font-semibold text-white leading-tight">Molly IA</div>
            <span className="text-[9px] text-gold tracking-widest uppercase block">Colegio Gemelli</span>
          </div>
        </div>
        <button onClick={() => router.push("/chat")}
          className="w-full bg-gold/10 border border-gold/[.22] rounded-xl px-3.5 py-2.5 text-gold-lt text-xs font-bold flex items-center gap-2 hover:bg-gold/20 transition-colors">
          ＋ Nueva conversación
        </button>
      </div>

      <div className="px-2 py-1">
        <p className="text-[9px] font-black text-white/20 tracking-[1.8px] uppercase px-3 py-3">Recientes</p>
        <div className="flex flex-col gap-0.5">
          {convs.map(c => (
            <button key={c.id} onClick={() => router.push(`/chat/${c.id}`)}
              className={`text-left px-3 py-2.5 rounded-xl transition-all ${activeId === c.id ? "bg-gold/10 border-l-2 border-gold" : "hover:bg-white/[.06]"}`}>
              <div className="text-xs text-white/70 font-medium truncate">{c.title}</div>
              <div className="text-[10px] text-white/28 mt-0.5">{new Date(c.updated_at).toLocaleDateString("es-CO")}</div>
            </button>
          ))}
        </div>
      </div>

      <div className="mt-auto p-4 border-t border-white/[.05] flex items-center gap-2.5">
        <div className="w-8 h-8 rounded-full bg-emerald flex items-center justify-center text-xs font-black text-white flex-shrink-0">
          {user?.firstName?.[0]}{user?.lastName?.[0]}
        </div>
        <div className="min-w-0 flex-1">
          <div className="text-xs font-bold text-white/80 truncate">{user?.fullName}</div>
          <div className="text-[10px] text-white/30">Padre de familia</div>
        </div>
      </div>
    </aside>
  );
}
```

- [ ] **Step 3: Crear chat layout.tsx**

```typescript
// frontend/app/chat/layout.tsx
import ChatSidebar from "@/components/chat/ChatSidebar";

export default function ChatLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex h-screen bg-cream">
      <ChatSidebar />
      <div className="flex-1 flex flex-col min-w-0">{children}</div>
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add frontend/app/(auth)/ frontend/app/chat/layout.tsx frontend/components/chat/ChatSidebar.tsx
git commit -m "feat: sign-in page, chat layout, sidebar with conversation history"
```

---

### Task 10: Chat principal — mensajes, input, streaming

**Files:**
- Create: `frontend/app/chat/page.tsx`
- Create: `frontend/app/chat/[id]/page.tsx`
- Create: `frontend/components/chat/ChatMessages.tsx`
- Create: `frontend/components/chat/MessageBubble.tsx`
- Create: `frontend/components/chat/SourcePills.tsx`
- Create: `frontend/components/chat/ChatInput.tsx`

- [ ] **Step 1: Crear SourcePills.tsx**

```typescript
// frontend/components/chat/SourcePills.tsx
const SOURCE_ICONS: Record<string, string> = {
  pdf: "📄", docx: "📄", web: "🌐", facebook: "📘", instagram: "📸", youtube: "▶️"
};

export default function SourcePills({ sources }: { sources: string[] }) {
  if (!sources.length) return null;
  return (
    <div className="flex gap-1.5 flex-wrap mt-2">
      {sources.map(s => (
        <span key={s} className="text-[10px] bg-emerald/[.08] text-emerald border border-emerald/[.14] rounded-full px-2.5 py-1 font-bold">
          {SOURCE_ICONS[s] ?? "📎"} {s}
        </span>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: Crear MessageBubble.tsx**

```typescript
// frontend/components/chat/MessageBubble.tsx
import MollyAvatar from "@/components/ui/MollyAvatar";
import SourcePills from "./SourcePills";
import type { Message } from "@/lib/types";

export default function MessageBubble({ msg }: { msg: Message }) {
  const isUser = msg.role === "user";
  const sources = JSON.parse(msg.sources_json || "[]") as string[];
  return (
    <div className={`flex gap-2.5 items-start ${isUser ? "flex-row-reverse" : ""}`}>
      {isUser
        ? <div className="w-[34px] h-[34px] rounded-xl bg-navy flex items-center justify-center text-xs font-black text-white flex-shrink-0">Tú</div>
        : <MollyAvatar size={34} />
      }
      <div className="max-w-[66%]">
        <p className={`text-[10px] font-black tracking-[.6px] uppercase text-muted mb-1 ${isUser ? "text-right" : ""}`}>
          {isUser ? "Tú" : "Molly IA"}
        </p>
        <div className={`px-4 py-3 rounded-[17px] text-[13.5px] leading-relaxed whitespace-pre-wrap ${
          isUser ? "bg-gradient-to-br from-navy to-navy-lt text-white/92 rounded-br-[3px]"
                 : "bg-white border border-cream-d text-gray-800 rounded-bl-[3px] shadow-sm"
        }`}>
          {msg.content}
        </div>
        {!isUser && <SourcePills sources={sources} />}
        <p className={`text-[10px] text-muted mt-1 opacity-55 ${isUser ? "text-right" : ""}`}>
          {new Date(msg.created_at).toLocaleTimeString("es-CO", { hour: "2-digit", minute: "2-digit" })}
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Crear ChatMessages.tsx**

```typescript
// frontend/components/chat/ChatMessages.tsx
"use client";
import { useEffect, useRef } from "react";
import MessageBubble from "./MessageBubble";
import MollyAvatar from "@/components/ui/MollyAvatar";
import type { Message } from "@/lib/types";

interface Props { messages: Message[]; loading?: boolean; }

export default function ChatMessages({ messages, loading }: Props) {
  const bottomRef = useRef<HTMLDivElement>(null);
  useEffect(() => { bottomRef.current?.scrollIntoView({ behavior: "smooth" }); }, [messages, loading]);

  if (!messages.length && !loading) {
    return (
      <div className="flex-1 flex flex-col items-center justify-center gap-4 p-10">
        <MollyAvatar size={90} float />
        <h2 className="font-display text-2xl font-semibold text-navy">¡Hola! Soy Molly</h2>
        <p className="text-muted text-sm text-center max-w-xs leading-relaxed font-light">
          Pregúntame sobre matrículas, horarios, eventos o cualquier información del Colegio Gemelli.
        </p>
        <div className="flex gap-2 flex-wrap justify-center mt-2">
          {["¿Cuándo son las matrículas?","¿Cuál es el horario de secretaría?","¿Qué eventos hay este mes?"].map(q => (
            <span key={q} className="bg-white border border-cream-d rounded-full px-4 py-2 text-xs text-navy font-semibold cursor-pointer hover:border-gold hover:text-gold transition-colors">{q}</span>
          ))}
        </div>
      </div>
    );
  }

  return (
    <div className="flex-1 overflow-y-auto p-7 flex flex-col gap-5 scrollbar-thin">
      {messages.map(m => <MessageBubble key={m.id} msg={m} />)}
      {loading && (
        <div className="flex gap-2.5 items-start">
          <MollyAvatar size={34} />
          <div className="bg-white border border-cream-d rounded-[17px] rounded-bl-[3px] px-4 py-3 flex gap-1.5">
            {[0,1,2].map(i => <span key={i} className="w-[7px] h-[7px] rounded-full bg-gold animate-bounce" style={{animationDelay:`${i*0.15}s`}} />)}
          </div>
        </div>
      )}
      <div ref={bottomRef} />
    </div>
  );
}
```

- [ ] **Step 4: Crear ChatInput.tsx**

```typescript
// frontend/components/chat/ChatInput.tsx
"use client";
import { useState, useRef } from "react";
import { Send } from "lucide-react";

interface Props { onSend: (msg: string) => void; disabled?: boolean; }

export default function ChatInput({ onSend, disabled }: Props) {
  const [value, setValue] = useState("");
  const ref = useRef<HTMLTextAreaElement>(null);

  const handleSend = () => {
    const msg = value.trim();
    if (!msg || disabled) return;
    onSend(msg);
    setValue("");
    if (ref.current) ref.current.style.height = "auto";
  };

  return (
    <div className="p-5 bg-white border-t border-cream-d">
      <div className="flex gap-2.5 items-end bg-cream border-[1.5px] border-cream-d rounded-2xl px-3.5 py-3 focus-within:border-gold transition-colors">
        <textarea
          ref={ref}
          value={value}
          onChange={e => { setValue(e.target.value); e.target.style.height = "auto"; e.target.style.height = `${Math.min(e.target.scrollHeight, 110)}px`; }}
          onKeyDown={e => { if (e.key === "Enter" && !e.shiftKey) { e.preventDefault(); handleSend(); } }}
          placeholder="Escribe tu pregunta sobre el colegio..."
          rows={1}
          disabled={disabled}
          className="flex-1 bg-transparent outline-none resize-none font-sans text-sm text-gray-800 placeholder:text-muted/60 min-h-[22px] max-h-[110px]"
        />
        <button onClick={handleSend} disabled={disabled || !value.trim()}
          className="w-[38px] h-[38px] bg-navy rounded-xl flex items-center justify-center flex-shrink-0 hover:bg-gold disabled:opacity-40 disabled:cursor-not-allowed transition-colors">
          <Send size={15} className="text-white" />
        </button>
      </div>
      <p className="text-center text-[10px] text-muted/55 mt-2">Molly responde únicamente con información oficial del Colegio Gemelli</p>
    </div>
  );
}
```

- [ ] **Step 5: Crear app/chat/[id]/page.tsx**

```typescript
// frontend/app/chat/[id]/page.tsx
"use client";
import { useEffect, useState } from "react";
import { useParams } from "next/navigation";
import { useApi } from "@/lib/api";
import MollyAvatar from "@/components/ui/MollyAvatar";
import ChatMessages from "@/components/chat/ChatMessages";
import ChatInput from "@/components/chat/ChatInput";
import type { Message } from "@/lib/types";

export default function ChatConversationPage() {
  const { id } = useParams<{ id: string }>();
  const api = useApi();
  const [messages, setMessages] = useState<Message[]>([]);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    api.getMessages(Number(id)).then(setMessages).catch(console.error);
  }, [id]);

  const handleSend = async (text: string) => {
    const optimistic: Message = {
      id: Date.now(), role: "user", content: text,
      sources_json: "[]", created_at: new Date().toISOString()
    };
    setMessages(prev => [...prev, optimistic]);
    setLoading(true);
    try {
      const res = await api.sendMessage(text, Number(id));
      const assistantMsg: Message = {
        id: res.message_id, role: "assistant", content: res.answer,
        sources_json: JSON.stringify(res.sources), created_at: new Date().toISOString()
      };
      setMessages(prev => [...prev, assistantMsg]);
    } catch (e) {
      console.error(e);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="flex flex-col h-full">
      <div className="px-7 py-4 bg-white border-b border-cream-d flex items-center justify-between">
        <div className="flex items-center gap-3">
          <MollyAvatar size={40} />
          <div>
            <div className="font-display text-xl font-semibold text-navy">Molly IA</div>
            <div className="text-xs text-emerald-lt flex items-center gap-1.5">
              <span className="w-1.5 h-1.5 rounded-full bg-emerald-lt" />Respondiendo con info actualizada
            </div>
          </div>
        </div>
        <span className="text-xs bg-gold/10 text-gold border border-gold/20 rounded-full px-3 py-1 font-bold">📚 5 fuentes activas</span>
      </div>
      <ChatMessages messages={messages} loading={loading} />
      <ChatInput onSend={handleSend} disabled={loading} />
    </div>
  );
}
```

- [ ] **Step 6: Crear app/chat/page.tsx (new chat)**

```typescript
// frontend/app/chat/page.tsx
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { useApi } from "@/lib/api";
import ChatMessages from "@/components/chat/ChatMessages";
import ChatInput from "@/components/chat/ChatInput";
import MollyAvatar from "@/components/ui/MollyAvatar";
import type { Message } from "@/lib/types";

export default function NewChatPage() {
  const router = useRouter();
  const api = useApi();
  const [loading, setLoading] = useState(false);

  const handleSend = async (text: string) => {
    setLoading(true);
    try {
      const res = await api.sendMessage(text);
      router.push(`/chat/${res.conversation_id}`);
    } catch (e) {
      console.error(e);
      setLoading(false);
    }
  };

  return (
    <div className="flex flex-col h-full">
      <div className="px-7 py-4 bg-white border-b border-cream-d flex items-center gap-3">
        <MollyAvatar size={40} />
        <div>
          <div className="font-display text-xl font-semibold text-navy">Molly IA</div>
          <div className="text-xs text-emerald-lt flex items-center gap-1.5">
            <span className="w-1.5 h-1.5 rounded-full bg-emerald-lt" />En línea
          </div>
        </div>
      </div>
      <ChatMessages messages={[]} loading={loading} />
      <ChatInput onSend={handleSend} disabled={loading} />
    </div>
  );
}
```

- [ ] **Step 7: Probar chat end-to-end**

Con backend corriendo en `:8000` y frontend en `:3000`:
1. Login con Google en `/sign-in`
2. Ir a `/chat`
3. Enviar una pregunta
4. Verificar: respuesta aparece con source pills, se guarda en sidebar

- [ ] **Step 8: Commit**

```bash
git add frontend/app/chat/ frontend/components/chat/
git commit -m "feat: chat page — messages, input, optimistic UI, source pills"
```

---

## FASE 5 — Panel Admin

### Task 11: Admin layout + dashboard KPIs + documentos

**Files:**
- Create: `frontend/app/admin/layout.tsx`
- Create: `frontend/app/admin/page.tsx`
- Create: `frontend/app/admin/documentos/page.tsx`
- Create: `frontend/components/admin/AdminSidebar.tsx`
- Create: `frontend/components/admin/KpiCard.tsx`
- Create: `frontend/components/admin/UploadZone.tsx`
- Create: `frontend/components/admin/DocumentsTable.tsx`
- Create: `frontend/components/admin/SourcesGrid.tsx`

- [ ] **Step 1: Crear AdminSidebar.tsx**

```typescript
// frontend/components/admin/AdminSidebar.tsx
"use client";
import { usePathname, useRouter } from "next/navigation";
import MollyAvatar from "@/components/ui/MollyAvatar";

const NAV = [
  { group: "Principal", items: [
    { icon: "📊", label: "Dashboard", href: "/admin" },
    { icon: "📤", label: "Documentos", href: "/admin/documentos" },
    { icon: "🔄", label: "Re-indexar", href: "/admin/fuentes" },
  ]},
  { group: "Fuentes", items: [
    { icon: "🌐", label: "Sitio Web", href: "/admin/fuentes" },
    { icon: "📘", label: "Facebook", href: "/admin/fuentes" },
    { icon: "📸", label: "Instagram", href: "/admin/fuentes" },
    { icon: "▶️", label: "YouTube", href: "/admin/fuentes" },
  ]},
  { group: "Análisis", items: [
    { icon: "💬", label: "Conversaciones", href: "/admin/conversaciones" },
    { icon: "❓", label: "Preguntas frecuentes", href: "/admin/frecuentes" },
  ]},
  { group: "Sistema", items: [
    { icon: "👥", label: "Usuarios", href: "/admin/usuarios" },
  ]},
];

export default function AdminSidebar() {
  const pathname = usePathname();
  const router = useRouter();
  return (
    <aside className="w-[232px] bg-navy border-r border-gold/[.10] flex flex-col">
      <div className="p-5 border-b border-white/[.05] flex items-center gap-2.5">
        <MollyAvatar size={32} />
        <div>
          <div className="font-display text-base font-semibold text-white leading-tight">Panel Admin</div>
          <div className="text-[9px] text-gold tracking-widest uppercase">Molly IA · Gemelli</div>
        </div>
      </div>
      <nav className="flex-1 p-2.5 overflow-y-auto">
        {NAV.map(g => (
          <div key={g.group}>
            <p className="text-[9px] font-black text-white/20 tracking-[2px] uppercase px-2.5 pt-3 pb-1.5">{g.group}</p>
            {g.items.map(item => (
              <button key={item.label} onClick={() => router.push(item.href)}
                className={`w-full flex items-center gap-2.5 px-3 py-2.5 rounded-xl text-xs font-semibold mb-0.5 transition-all ${pathname === item.href ? "bg-gold/10 text-gold-lt" : "text-white/50 hover:bg-white/[.05] hover:text-white/80"}`}>
                <span className="text-[15px] w-[18px] text-center">{item.icon}</span>{item.label}
              </button>
            ))}
          </div>
        ))}
      </nav>
    </aside>
  );
}
```

- [ ] **Step 2: Crear KpiCard.tsx**

```typescript
// frontend/components/admin/KpiCard.tsx
const COLORS = ["from-gold to-gold-lt", "from-emerald to-emerald-lt", "from-navy-mid to-navy-lt", "from-sand to-cream-d"];

interface Props { value: string | number; label: string; delta?: string; up?: boolean; index?: number; }

export default function KpiCard({ value, label, delta, up, index = 0 }: Props) {
  return (
    <div className="bg-white border border-cream-d rounded-2xl p-5 relative overflow-hidden">
      <div className={`absolute top-0 left-0 right-0 h-[3px] bg-gradient-to-r ${COLORS[index % 4]}`} />
      <div className="font-display text-[40px] font-semibold text-navy leading-none">{value}</div>
      <div className="text-[11px] text-muted uppercase tracking-[.5px] mt-1">{label}</div>
      {delta && <div className={`text-xs font-bold mt-2 ${up ? "text-emerald" : "text-red-500"}`}>{up ? "↑" : "↓"} {delta}</div>}
    </div>
  );
}
```

- [ ] **Step 3: Crear UploadZone.tsx**

```typescript
// frontend/components/admin/UploadZone.tsx
"use client";
import { useRef, useState } from "react";

interface Props { onUpload: (file: File, category: string) => Promise<void>; }

export default function UploadZone({ onUpload }: Props) {
  const inputRef = useRef<HTMLInputElement>(null);
  const [dragging, setDragging] = useState(false);
  const [uploading, setUploading] = useState(false);

  const handle = async (file: File) => {
    setUploading(true);
    await onUpload(file, "general");
    setUploading(false);
  };

  return (
    <div
      onClick={() => inputRef.current?.click()}
      onDragOver={e => { e.preventDefault(); setDragging(true); }}
      onDragLeave={() => setDragging(false)}
      onDrop={e => { e.preventDefault(); setDragging(false); const f = e.dataTransfer.files[0]; if (f) handle(f); }}
      className={`bg-white border-2 border-dashed rounded-2xl p-9 text-center cursor-pointer transition-all ${dragging ? "border-gold bg-gold/[.02]" : "border-cream-d hover:border-gold hover:bg-gold/[.01]"}`}
    >
      <input ref={inputRef} type="file" accept=".pdf,.docx,.txt" className="hidden"
        onChange={e => { const f = e.target.files?.[0]; if (f) handle(f); }} />
      <div className="text-4xl mb-3">{uploading ? "⏳" : "📎"}</div>
      <div className="font-display text-[22px] font-semibold text-navy mb-1.5">{uploading ? "Procesando..." : "Arrastra documentos aquí"}</div>
      <p className="text-xs text-muted">O haz clic para seleccionar. Molly los aprenderá en segundos.</p>
      <div className="flex gap-2 justify-center mt-3">
        {["PDF","DOCX","TXT"].map(t => (
          <span key={t} className="bg-cream border border-cream-d rounded-md px-2.5 py-1 text-[10px] font-black text-muted">{t}</span>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Crear DocumentsTable.tsx**

```typescript
// frontend/components/admin/DocumentsTable.tsx
"use client";
import type { Document } from "@/lib/types";

const STATUS = {
  indexed:    "bg-emerald/10 text-emerald",
  processing: "bg-gold/10 text-gold",
  error:      "bg-red-500/10 text-red-600",
};
const STATUS_LABEL = { indexed: "✓ Indexado", processing: "⟳ Procesando", error: "✗ Error" };

interface Props { docs: Document[]; onDelete: (id: number) => void; }

export default function DocumentsTable({ docs, onDelete }: Props) {
  return (
    <div className="bg-white border border-cream-d rounded-2xl overflow-hidden">
      <div className="px-5 py-4 border-b border-cream-d flex items-center justify-between">
        <h3 className="font-display text-lg font-semibold text-navy">Documentos activos</h3>
        <span className="text-xs bg-gold/10 text-gold border border-gold/20 rounded-full px-3 py-1 font-bold">{docs.length} archivos</span>
      </div>
      <table className="w-full text-xs border-collapse">
        <thead>
          <tr className="bg-cream-d/40 border-b border-cream-d">
            {["Documento","Categoría","Subido","Estado",""].map(h => (
              <th key={h} className="text-left px-5 py-3 text-[10px] font-black uppercase tracking-[.5px] text-muted">{h}</th>
            ))}
          </tr>
        </thead>
        <tbody>
          {docs.map(d => (
            <tr key={d.id} className="border-b border-cream-d/60 hover:bg-cream/70 transition-colors">
              <td className="px-5 py-3 font-bold text-navy flex items-center gap-2">📄 {d.name}</td>
              <td className="px-5 py-3 text-muted capitalize">{d.category}</td>
              <td className="px-5 py-3 text-muted">{new Date(d.created_at).toLocaleDateString("es-CO")}</td>
              <td className="px-5 py-3">
                <span className={`inline-flex items-center gap-1 text-[10px] font-black px-2.5 py-1 rounded-full ${STATUS[d.status]}`}>
                  {STATUS_LABEL[d.status]}
                </span>
              </td>
              <td className="px-5 py-3">
                <button onClick={() => onDelete(d.id)}
                  className="border border-cream-d rounded-lg px-2.5 py-1 text-muted hover:border-red-400 hover:text-red-500 transition-colors font-semibold">
                  Eliminar
                </button>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

- [ ] **Step 5: Crear SourcesGrid.tsx**

```typescript
// frontend/components/admin/SourcesGrid.tsx
"use client";
import { useState } from "react";
import { useApi } from "@/lib/api";

const SOURCES = [
  { key: "web", icon: "🌐", name: "colgemelli.edu.co", info: "Scraping semanal" },
  { key: "facebook", icon: "📘", name: "Facebook", info: "Posts públicos" },
  { key: "instagram", icon: "📸", name: "Instagram", info: "Captions públicas" },
  { key: "youtube", icon: "▶️", name: "YouTube", info: "Títulos + descripciones" },
];

export default function SourcesGrid() {
  const api = useApi();
  const [loading, setLoading] = useState<string | null>(null);

  const trigger = async (key: string) => {
    setLoading(key);
    try { await api.triggerScrape(key); } catch (e) { console.error(e); }
    finally { setLoading(null); }
  };

  return (
    <div className="grid grid-cols-4 gap-4 mb-5">
      {SOURCES.map(s => (
        <div key={s.key} className="bg-white border border-cream-d rounded-xl p-4">
          <div className="flex items-center justify-between mb-1.5">
            <span className="text-2xl">{s.icon}</span>
            <span className="w-2 h-2 rounded-full bg-emerald-lt" />
          </div>
          <div className="text-xs font-bold text-navy">{s.name}</div>
          <div className="text-[10px] text-muted mb-3">{s.info}</div>
          <button onClick={() => trigger(s.key)} disabled={loading === s.key}
            className="w-full text-[10px] font-bold border border-cream-d rounded-lg py-1.5 text-muted hover:border-navy hover:text-navy transition-colors disabled:opacity-50">
            {loading === s.key ? "Scrapeando..." : "Re-scrapear"}
          </button>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 6: Crear admin/layout.tsx**

```typescript
// frontend/app/admin/layout.tsx
import AdminSidebar from "@/components/admin/AdminSidebar";

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex h-screen bg-cream">
      <AdminSidebar />
      <main className="flex-1 overflow-y-auto p-8">{children}</main>
    </div>
  );
}
```

- [ ] **Step 7: Crear admin/page.tsx (Dashboard)**

```typescript
// frontend/app/admin/page.tsx
"use client";
import { useEffect, useState } from "react";
import { useApi } from "@/lib/api";
import KpiCard from "@/components/admin/KpiCard";
import SourcesGrid from "@/components/admin/SourcesGrid";
import type { Stats } from "@/lib/types";

export default function AdminDashboard() {
  const api = useApi();
  const [stats, setStats] = useState<Stats | null>(null);

  useEffect(() => { api.getStats().then(setStats).catch(console.error); }, []);

  const kpis = stats ? [
    { value: stats.docs_indexed, label: "Docs indexados", delta: "activos en Molly", up: true },
    { value: stats.questions_answered, label: "Preguntas respondidas", delta: "total histórico", up: true },
    { value: `${Math.round((stats.questions_answered / Math.max(stats.questions_answered + 5, 1)) * 100)}%`, label: "Tasa de respuesta", up: true },
    { value: stats.active_users, label: "Padres registrados", up: true },
  ] : [];

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <div>
          <h1 className="font-display text-[32px] font-semibold text-navy">Dashboard</h1>
          <p className="text-xs text-muted mt-1">Estado general de Molly IA</p>
        </div>
      </div>
      <div className="grid grid-cols-4 gap-4 mb-6">
        {kpis.map((k, i) => <KpiCard key={k.label} {...k} index={i} />)}
      </div>
      <h2 className="font-display text-xl font-semibold text-navy mb-4">Estado de fuentes</h2>
      <SourcesGrid />
    </div>
  );
}
```

- [ ] **Step 8: Crear admin/documentos/page.tsx**

```typescript
// frontend/app/admin/documentos/page.tsx
"use client";
import { useEffect, useState } from "react";
import { useApi } from "@/lib/api";
import UploadZone from "@/components/admin/UploadZone";
import DocumentsTable from "@/components/admin/DocumentsTable";
import type { Document } from "@/lib/types";

export default function DocumentosPage() {
  const api = useApi();
  const [docs, setDocs] = useState<Document[]>([]);

  const load = () => api.getDocuments().then(setDocs).catch(console.error);
  useEffect(() => { load(); }, []);

  const { getToken } = useAuth();

  const handleUpload = async (file: File, category: string) => {
    const token = await getToken();
    const form = new FormData();
    form.append("file", file);
    form.append("category", category);
    await fetch(`${process.env.NEXT_PUBLIC_API_URL}/documents`, {
      method: "POST",
      headers: { Authorization: `Bearer ${token}` },
      body: form,
    });
    await load();
  };

  const handleDelete = async (id: number) => {
    await api.deleteDocument(id);
    setDocs(prev => prev.filter(d => d.id !== id));
  };

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <div>
          <h1 className="font-display text-[32px] font-semibold text-navy">Documentos de Molly</h1>
          <p className="text-xs text-muted mt-1">Gestiona los archivos que Molly usa para responder</p>
        </div>
      </div>
      <UploadZone onUpload={handleUpload} />
      <div className="mt-5">
        <DocumentsTable docs={docs} onDelete={handleDelete} />
      </div>
    </div>
  );
}
```

- [ ] **Step 9: Verificar panel admin**

Asignar rol admin en Clerk dashboard → `publicMetadata: {"role":"admin"}`. Ir a `/admin` — verificar KPIs, fuentes, documentos.

- [ ] **Step 10: Commit**

```bash
git add frontend/app/admin/ frontend/components/admin/
git commit -m "feat: admin panel — dashboard KPIs, document upload, sources scraping"
```

---

## FASE 6 — Deployment

### Task 12: Dockerizar backend + configurar Railway

**Files:**
- Create: `backend/Dockerfile`
- Create: `backend/railway.toml`

- [ ] **Step 1: Crear Dockerfile del backend**

```dockerfile
# backend/Dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Pre-descargar el modelo de embeddings para evitar cold start lento
RUN python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('paraphrase-multilingual-MiniLM-L12-v2')"

COPY . .
CMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

- [ ] **Step 2: Crear railway.toml**

```toml
# backend/railway.toml
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "alembic upgrade head && uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60

[[volumes]]
mountPath = "/app/chroma_db"
```

- [ ] **Step 3: Subir a GitHub**

```bash
cd /Users/jm/molly-ia
git remote add origin https://github.com/TU_USUARIO/molly-ia.git
git push -u origin main
```

- [ ] **Step 4: Crear proyecto Railway**

1. Ir a `railway.app` → New Project → Deploy from GitHub → seleccionar `molly-ia`
2. Agregar servicio PostgreSQL → copiar `DATABASE_URL`
3. Agregar todas las variables de entorno del `.env.example` con valores reales
4. Railway detecta el Dockerfile y hace deploy automático

Expected: `https://molly-ia-backend.up.railway.app/health` → `{"status":"ok"}`

- [ ] **Step 5: Correr migración en Railway**

```bash
# En Railway → tu servicio → Shell
alembic upgrade head
```

- [ ] **Step 6: Deploy frontend en Vercel**

1. Ir a `vercel.com` → New Project → importar repo → seleccionar carpeta `frontend`
2. Agregar variables de entorno:
   - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`
   - `CLERK_SECRET_KEY`
   - `NEXT_PUBLIC_API_URL` = URL de Railway (ej: `https://molly-ia-backend.up.railway.app`)
   - `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in`
   - `NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat`
3. Deploy

Expected: `https://molly-ia.vercel.app` muestra landing con Molly.

- [ ] **Step 7: Actualizar CORS en main.py con URL de Vercel**

```python
# backend/main.py — actualizar allow_origins
allow_origins=[
    "http://localhost:3000",
    "https://molly-ia.vercel.app",    # reemplazar con tu URL real
    "https://*.vercel.app",
],
```

```bash
git add backend/main.py
git commit -m "fix: add Vercel URL to CORS allowlist"
git push
```

- [ ] **Step 8: Ingestar datos iniciales del colegio**

```bash
# Desde el panel admin en producción:
# 1. Ir a /admin/documentos → subir PDFs del colegio
# 2. Ir a /admin/fuentes → click "Re-scrapear" en Sitio Web
# 3. Repetir para Facebook, Instagram, YouTube
```

- [ ] **Step 9: Smoke test en producción**

1. Registrarse como padre en `https://molly-ia.vercel.app`
2. Preguntar: "¿Cuándo son las matrículas?"
3. Verificar: respuesta coherente con información del colegio + sources citadas
4. Verificar panel admin: docs indexados, preguntas respondidas aumentan

- [ ] **Step 10: Commit final**

```bash
git add backend/Dockerfile backend/railway.toml
git commit -m "feat: Docker + Railway config, production deployment ready"
git push
```

---

### Task 13: Páginas admin secundarias + perfil

**Files:**
- Create: `frontend/app/admin/fuentes/page.tsx`
- Create: `frontend/app/admin/conversaciones/page.tsx`
- Create: `frontend/app/admin/frecuentes/page.tsx`
- Create: `frontend/app/admin/usuarios/page.tsx`
- Create: `frontend/app/perfil/page.tsx`

- [ ] **Step 1: Crear admin/fuentes/page.tsx**

```typescript
// frontend/app/admin/fuentes/page.tsx
"use client";
import SourcesGrid from "@/components/admin/SourcesGrid";

export default function FuentesPage() {
  return (
    <div>
      <h1 className="font-display text-[32px] font-semibold text-navy mb-2">Fuentes de datos</h1>
      <p className="text-xs text-muted mb-6">Re-indexa cada fuente para que Molly tenga información actualizada</p>
      <SourcesGrid />
    </div>
  );
}
```

- [ ] **Step 2: Crear admin/conversaciones/page.tsx**

```typescript
// frontend/app/admin/conversaciones/page.tsx
"use client";
import { useEffect, useState } from "react";
import { useApi } from "@/lib/api";

export default function ConversacionesPage() {
  return (
    <div>
      <h1 className="font-display text-[32px] font-semibold text-navy mb-2">Conversaciones</h1>
      <p className="text-xs text-muted mb-6">Log de interacciones de padres con Molly</p>
      <div className="bg-white border border-cream-d rounded-2xl p-8 text-center text-muted">
        <div className="text-4xl mb-3">💬</div>
        <p className="font-display text-xl font-semibold text-navy mb-2">Próximamente</p>
        <p className="text-sm">El historial de conversaciones anónimo estará disponible en la siguiente versión.</p>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Crear admin/frecuentes/page.tsx**

```typescript
// frontend/app/admin/frecuentes/page.tsx
export default function FrecuentesPage() {
  return (
    <div>
      <h1 className="font-display text-[32px] font-semibold text-navy mb-2">Preguntas frecuentes</h1>
      <p className="text-xs text-muted mb-6">Las preguntas que más hacen los padres a Molly</p>
      <div className="bg-white border border-cream-d rounded-2xl p-8 text-center text-muted">
        <div className="text-4xl mb-3">❓</div>
        <p className="font-display text-xl font-semibold text-navy mb-2">Próximamente</p>
        <p className="text-sm">El análisis de preguntas frecuentes se habilitará cuando haya suficientes conversaciones.</p>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Crear admin/usuarios/page.tsx**

```typescript
// frontend/app/admin/usuarios/page.tsx
export default function UsuariosPage() {
  return (
    <div>
      <h1 className="font-display text-[32px] font-semibold text-navy mb-2">Usuarios</h1>
      <p className="text-xs text-muted mb-6">Padres de familia registrados en Molly IA</p>
      <div className="bg-white border border-cream-d rounded-2xl p-6">
        <p className="text-sm text-muted text-center py-4">
          Gestiona los usuarios directamente en el{" "}
          <a href="https://dashboard.clerk.com" target="_blank" rel="noopener noreferrer"
            className="text-gold font-bold hover:underline">Dashboard de Clerk</a>{" "}
          — ahí puedes ver, suspender o asignar roles de admin.
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Step 5: Crear perfil/page.tsx**

```typescript
// frontend/app/perfil/page.tsx
import { UserProfile } from "@clerk/nextjs";

export default function PerfilPage() {
  return (
    <div className="min-h-screen bg-cream flex items-center justify-center p-8">
      <UserProfile />
    </div>
  );
}
```

- [ ] **Step 6: Agregar import useAuth en documentos/page.tsx**

Verificar que el archivo `frontend/app/admin/documentos/page.tsx` tenga el import correcto:
```typescript
import { useAuth } from "@clerk/nextjs";
```

- [ ] **Step 7: Commit**

```bash
git add frontend/app/admin/fuentes/ frontend/app/admin/conversaciones/ \
        frontend/app/admin/frecuentes/ frontend/app/admin/usuarios/ \
        frontend/app/perfil/
git commit -m "feat: remaining admin pages + perfil page"
```

---

## Checklist de cuentas externas a crear ANTES de empezar

| Servicio | URL | Qué crear |
|----------|-----|-----------|
| Groq | console.groq.com | Cuenta gratuita → API Keys → Create key |
| Clerk | clerk.com | New application → Enable Google + Email → Copy keys |
| Cloudflare R2 | cloudflare.com → R2 | Bucket `molly-docs` → API Token con permisos R2 |
| Railway | railway.app | New project (GitHub) + PostgreSQL service |
| Vercel | vercel.com | Import GitHub repo → set env vars |
| YouTube API | console.cloud.google.com | New project → Enable YouTube Data API v3 → Create API key |

---

## Variables de entorno finales

```bash
# backend/.env
DATABASE_URL=postgresql://...              # de Railway
CHROMA_PERSIST_DIR=/app/chroma_db         # volumen Railway
GROQ_API_KEY=gsk_...                       # de Groq
CLERK_SECRET_KEY=sk_live_...               # de Clerk
CLERK_PUBLISHABLE_KEY=pk_live_...          # de Clerk
R2_ACCOUNT_ID=abc123                       # Cloudflare account ID
R2_ACCESS_KEY_ID=...                       # R2 API token
R2_SECRET_ACCESS_KEY=...                   # R2 API secret
R2_BUCKET_NAME=molly-docs
YOUTUBE_API_KEY=AIza...                    # Google Cloud
FACEBOOK_PAGE_ID=ColegioGemelli           # nombre de la página FB
INSTAGRAM_USERNAME=colegiogemelli          # handle de Instagram
```
